# Task: The postfix and prefix forms

### 📝 Task Details

* **Original Task:** [The postfix and prefix forms](https://javascript.info/operators#postfix-and-prefix-forms)
* **Importance:** 5

> What are the final values of all variables `a`, `b`, `c` and `d` after the code below?
>
> ```javascript
> let a = 1, b = 1;
>
> let c = ++a; // ?
> let d = b++; // ?
> ```
