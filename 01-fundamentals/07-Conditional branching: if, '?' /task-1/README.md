# Task: if (a string with zero)

### 📝 Task Details

* **Original Task:** [if (a string with zero)](https://javascript.info/ifelse#if-a-string-with-zero)
* **Importance:** 5

> Will `alert` be shown?
>
> ```javascript
> if ("0") {
>   alert( 'Hello' );
> }
> ```
