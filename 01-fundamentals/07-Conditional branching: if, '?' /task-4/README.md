# Task: Rewrite 'if' into '?'

### 📝 Task Details

* **Original Task:** [Rewrite 'if' into '?'](https://javascript.info/ifelse#rewrite-if-into)
* **Importance:** 5

> Rewrite this `if` using the conditional operator `?`:
>
> ```javascript
> let result;
>
> if (a + b < 4) {
>   result = 'Below';
> } else {
>   result = 'Over';
> }
> ```
