# Task: Function min(a, b)

### **📝 Task Details**

* **Original Task:** [Function min(a, b)](https://javascript.info/function-basics#function-min-a-b)
* **Importance:** 1

Write a function `min(a,b)` which returns the least of two numbers `a` and `b`.

For instance:

```javascript
min(2, 5) == 2
min(3, -1) == -1
min(1, 1) == 1
