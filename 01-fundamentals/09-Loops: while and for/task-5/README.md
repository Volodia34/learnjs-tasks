# Task: Replace "for" with "while"

### **📝 Task Details**

* **Original Task:** [Replace "for" with "while"](https://javascript.info/while-for#replace-for-with-while)
* **Importance:** 5

Rewrite the code changing the `for` loop to `while` without altering its behavior (the output should stay same).

**Original code:**
```javascript
for (let i = 0; i < 3; i++) {
  alert( `number ${i}!` );
}
