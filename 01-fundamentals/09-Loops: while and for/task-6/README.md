# Task: Repeat until the input is correct

### **📝 Task Details**

* **Original Task:** [Repeat until the input is correct](https://javascript.info/while-for#repeat-until-the-input-is-correct)
* **Importance:** 5

Write a loop which prompts for a number greater than `100`. If the visitor enters another number – ask them to input again.

The loop must ask for a number until either:
1. The visitor enters a number greater than `100`.
2. The visitor cancels the input or enters an empty line.

> **Note:** Assume that the visitor only inputs numbers. No need to handle non-numeric input.

```javascript
// Write your code here
